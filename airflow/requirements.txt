apache-airflow==3.1.3
# HTTP requests
requests>=2.31.0
# Environment variables
python-dotenv>=1.0.0

# pipeline dependencies are installed separately
# see versions/v1/requirements.txt
# see versions/v2/requirements.txt
